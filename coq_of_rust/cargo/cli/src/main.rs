// Stub binary target for crate_universe metadata resolution only — never
// compiled. Real source is fetched from upstream rocq-of-rust at build time.
// See coq_of_rust/cargo/BUILD.bazel.
fn main() {}
