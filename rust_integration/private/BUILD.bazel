# BUILD.bazel for rust_integration/private package
# Required for Bzlmod compatibility

package(default_visibility = ["//rust_integration:__subpackages__"])

exports_files(
    srcs = glob(["*.bzl"]),
)
